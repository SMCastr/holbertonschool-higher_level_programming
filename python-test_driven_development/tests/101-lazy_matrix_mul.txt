# Additional test cases
if __name__ == "__main__":
    try:
        # Ensure that the script works with NumPy
        import numpy as np

        ma = np.array([[1, 2], [3, 4]])
        mb = np.array([[5, 6], [7, 8]])
        result = lazy_matrix_mul(ma, mb)
        print("Correct output - case:", ma, "/", mb)
        print(result)
    except ImportError:
        print("You have to use the module numpy")

    try:
        # Test for incompatible matrix multiplication
        ma = [[1, 2], [3, 4]]
        mb = "Holberton"
        lazy_matrix_mul(ma, mb)
    except ValueError as ve:
        print(ve)

    try:
        # Test for incompatible matrix multiplication
        ma = [[5, 6], [7, 8]]
        mb = "Holberton"
        lazy_matrix_mul(ma, mb)
    except ValueError as ve:
        print(ve)

    # Additional test case: Test for matrices with different sizes
    ma = [[5, 6, 10], [7, 8]]
    mb = [[5, 6], [7, 8]]
    result = lazy_matrix_mul(ma, mb)
    print("Correct output - case:", ma, "/", mb)
    print(result)
